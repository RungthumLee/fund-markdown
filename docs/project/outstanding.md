---
title: Outstanding Items
tags: [project, outstanding, todo]
updated: 2026-08-27
---

# 📌 Outstanding Items

สิ่งที่ยัง**ค้างอยู่** ยังไม่ได้ตัดสินใจ หรือรู้ว่าไม่สมบูรณ์แต่ยอมรับไว้ก่อน
ต่างจาก [[issues|Issues]] ตรงที่นี่คือ "ยังไม่ได้ทำ" ไม่ใช่ "พังแล้วแก้"

**ที่เกี่ยวข้อง:** [[tasks|Tasks]] · [[roadmap|Roadmap]] · [[data-quality|Data Quality]]

ความสำคัญ: 🔴 สูง · 🟡 กลาง · 🟢 ต่ำ

---

## 🔴 OUT-001 — RMF ตรวจจับด้วย heuristic จากชื่อกองทุน

**สถานะ:** ยอมรับไว้ก่อน

API ไม่มี flag แยกสำหรับ RMF (`fund_class_tax_incentive_type` มีแค่ SSF และ Thai ESG)
ตอนนี้ตรวจจากคำว่า `RMF` ในชื่อย่อกองทุน แล้วติด tag `#tax/rmf`

**ความเสี่ยง:** กอง RMF ที่ชื่อย่อไม่มีคำว่า RMF จะถูกมองข้าม
และกองที่มีตัวอักษร RMF โดยบังเอิญจะถูกติด tag ผิด

**ทางแก้ที่เป็นไปได้:** parse จากข้อความใน factsheet PDF
หรือ cross-check กับรายชื่อ RMF ที่ AIMC เผยแพร่

---

## 🔴 OUT-002 — ยังไม่รู้เพดาน rate limit ที่แท้จริง

**สถานะ:** ใช้ค่าอนุรักษ์นิยมไปก่อน (0.12 วินาที/request)

ทดสอบยิงต่อเนื่องหลายพัน request แล้วไม่เจอ 429 แต่ยังไม่ได้ทดสอบหาเพดาน
และ SEC ไม่ได้ประกาศตัวเลขไว้

**ทางแก้:** ติดต่อ SEC หรือทดสอบแบบค่อย ๆ เร่งในช่วงเวลาที่ยอมให้พลาดได้
ดู [[issues|ISS-005]]

---

## 🟢 OUT-003 — Factsheet PDF ที่เป็นภาพสแกน — **ไม่พบปัญหา**

**สถานะ:** ตรวจแล้ว ไม่ต้องทำอะไร

`parse_factsheets.py` ตรวจทุกไฟล์ว่าแกะข้อความไทยได้หรือไม่
ผลจากการรันจริง: **2,119 จาก 2,119 ไฟล์แกะข้อความไทยได้ครบ 100%**
(`thai_text_ok: true` ทุกฉบับ) — ไม่มีไฟล์ใดเป็นภาพสแกน จึงยังไม่ต้องทำ OCR

**ยังคงไว้เพราะ:** การตรวจสอบยังอยู่ในสคริปต์ ถ้ารอบอัปเดตถัดไปเจอ PDF สแกน
จะขึ้น `thai_text_ok: false` และมีคำเตือนในโน้ตทันที

---

## 🟡 OUT-004 — ข้อมูลย้อนหลังถูกจำกัดหน้าต่างเวลา

**สถานะ:** ตั้งใจ (ดู [[decisions|DEC-002]])

NAV เก็บแค่ 120 วัน, portfolio 2 ไตรมาส
ทำให้วิเคราะห์แนวโน้มระยะยาว หรือคำนวณผลตอบแทนย้อนหลังเองไม่ได้

**ทางแก้:** เพิ่มค่าคงที่ใน `scripts/harvest.py` แล้วรัน `--force`
เตือน: NAV ย้อนหลัง 5 ปีของทุกกองน่าจะเป็นหลักล้านแถวและใช้เวลาหลายชั่วโมง

---

## 🟡 OUT-005 — ยังไม่มีการเปรียบเทียบข้ามกองทุน

vault ตอนนี้ให้ข้อมูล**ต่อกอง**และ**ดัชนีจัดกลุ่ม** แต่ยังไม่มี:
- ตารางเทียบค่าธรรมเนียมของกองประเภทเดียวกัน
- อันดับตาม Sharpe / Alpha ในแต่ละหมวด
- กราฟ/แผนภูมิ

**ทางแก้:** เพิ่ม Dataview query ในโน้ต index
(frontmatter ทุกโน้ตพร้อมรองรับแล้ว) หรือสร้างโน้ตเปรียบเทียบเพิ่ม

---

## 🟡 OUT-006 — บาง dataset มี coverage ต่ำกว่าที่คาด

`fs_statistics` คืนเพียง ~3,700 แถว เทียบกับ 4,892 share class ที่ยัง active
แปลว่ากองจำนวนมากไม่ได้รายงานสถิติ (น่าจะเป็นกองอายุน้อยกว่า 1 ปี
หรือกองตราสารหนี้ที่ไม่ต้องรายงาน Sharpe/Alpha)

**ยังไม่ได้ยืนยัน** ว่าเป็นเพราะเหตุนี้จริงหรือเป็นช่องว่างของข้อมูล
ตัวเลข coverage จริงอยู่ใน [[data-quality|Data Quality]]

---

## 🟢 OUT-007 — ยังไม่ได้ทำ incremental update

ตอนนี้การอัปเดตคือลบ `.done` แล้วดึงใหม่ทั้งหมด
ทั้งที่ API มี `last_upd_date` ที่ใช้ทำ delta ได้

**ทางแก้:** เก็บ `last_upd_date` สูงสุดของแต่ละ dataset
แล้วดึงเฉพาะที่ใหม่กว่า — แต่ API ไม่มี query param กรองด้วย `last_upd_date` โดยตรง
จึงอาจต้องดึงมาทั้งหมดแล้วเทียบอยู่ดี

---

## 🟢 OUT-008 — ยังไม่ได้ใช้ข้อมูล 4 endpoint อย่างเต็มที่

| Dataset | สถานะการใช้งาน |
|---|---|
| `fs_ipos` | harvest แล้ว แต่ยังไม่ได้แสดงใน vault (ข้อมูล IPO ของกองที่ตั้งแล้วมีประโยชน์จำกัด) |
| `out_portfolio` | แสดงแค่ 30 รายการแรก จากบางกองที่มีหลายร้อยรายการ |
| `dividend_history` | แสดง 20 ครั้งล่าสุด |
| `specifications` | แสดงเป็นรายการ code+desc ยังไม่ได้ตีความ |

---

## 🟢 OUT-009 — ตั้ง git repository — **เสร็จแล้ว**

**สถานะ:** ✅ push ขึ้น `github.com/RungthumLee/fund-markdown` (branch `main`) แล้ว

`.gitignore` ปรับให้ ignore ทั้ง `data/` (regenerate ได้ + `funds.json` 156MB
เกินลิมิต 100MB ของ GitHub) และ `.env.local` · commit เฉพาะ product:
`vault/` (markdown 245MB) + `scripts/` + `docs/` + `_spec/`

ก่อน push ยืนยันแล้ว: `git check-ignore` ผ่านทุกไฟล์ลับ · สแกน 8 ค่าลับใน
`.env.local` ไม่หลุดในไฟล์ที่ track เลย — ดู [[security-notes|Security Notes]]
